pub mod client;
pub mod cmdopt;
pub mod config;
pub mod server;
pub mod tls;
pub mod weirduri;

pub fn add(left: usize, right: usize) -> usize {
    left + right
}

#[cfg(test)]
mod tests {
    use super::*;

    #[test]
    fn it_works() {
        let result = add(2, 2);
        assert_eq!(result, 4);
    }
}
